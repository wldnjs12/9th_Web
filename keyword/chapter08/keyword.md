- **`Debounce`** 개념 정리 🍠
    
    ## Debounce란?
    
    이벤트가 여러 번 연속해서 발생해도,
    
    **마지막 이벤트가 발생한 후 일정 시간 동안 추가 입력이 없을 때 단 한 번만 함수가 실행되도록 만드는 기법**이다.
    
    ## Debounce가 필요한 이유
    
    - 과도한 입력 이벤트로 인한 성능 저하 방지
    - API 호출 횟수 감소
    - 입력이 잦은 UI(검색창 등)에서 부하를 줄이기 좋음
    - 스크롤/리사이즈 같은 잦은 이벤트 제어
    
    ## 동작 방식
    
    1. 이벤트가 발생하면 타이머를 초기화한다.
    2. 새로운 이벤트가 있을 때마다 타이머를 다시 리셋한다.
    3. 일정 시간 동안 이벤트가 더 이상 들어오지 않을 때 함수가 실행된다.
- **`Debounce`** 코드 작성 🍠
    
    ```tsx
    function debounce(fn, delay) {
      let timer = null;
    
      return function (...args) {
        clearTimeout(timer);
        timer = setTimeout(() => {
          fn.apply(this, args);
        }, delay);
      };
    }
    
    ```
    
-- **`Throttling`** 개념 정리 🍠
    
    ## Throttle이란?
    
    이벤트가 아무리 자주 발생해도 **정해둔 시간 동안에는 한 번만 함수가 실행되도록 제한하는 기법**이다.
    
    즉,
    
    - 일정 주기마다 **딱 한 번만 실행**
    - 그 주기 안에 추가 이벤트가 발생해도 무시
    
    Debounce가 "마지막 이벤트 기준"이라면,
    
    Throttle은 **시간 간격 기준**으로 실행을 제한한다.
    
    ## 왜 사용하는가?
    
    - 스크롤/리사이즈처럼 이벤트가 매우 자주 발생하는 경우
    - 계속 실행되면 렌더링·계산에 큰 부담이 생기는 작업
    - 일정 간격으로만 반응해도 되는 UI
    
    ## 동작 방식
    
    1. 특정 시간(window)이 시작되면 즉시 또는 첫 이벤트 때 실행된다.
    2. 그 시간이 끝날 때까지 들어오는 이벤트는 무시된다.
    3. 시간 간격이 끝나면 다음 이벤트가 다시 실행될 수 있다.
- **`Throttling`** 코드 작성 🍠
    
    ```tsx
    function throttle(fn, delay) {
      let lastCall = 0;
    
      return function (...args) {
        const now = Date.now();
    
        if (now - lastCall >= delay) {
          lastCall = now;
          fn.apply(this, args);
        }
      };
    }
    
    ```